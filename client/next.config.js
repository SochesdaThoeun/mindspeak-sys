/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'localhost', 'www.gravatar.com', 'api.mindspeak.xyz', '127.0.0.1'],
  },
}

module.exports = nextConfig 